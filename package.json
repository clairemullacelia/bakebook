{
  "name": "bakebook",
  "version": "1.0.0",
  "description": "bakebook — recipe development & storage app (Capacitor iOS wrapper of the web app in ./bakebook)",
  "private": true,
  "scripts": {
    "cap:sync": "cap sync ios",
    "cap:open": "cap open ios"
  },
  "dependencies": {
    "@capacitor-firebase/authentication": "^6.3.1",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@revenuecat/purchases-capacitor": "^9.2.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
